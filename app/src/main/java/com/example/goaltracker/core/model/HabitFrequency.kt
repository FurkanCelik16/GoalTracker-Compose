package com.example.goaltracker.core.model

enum class HabitFrequency {
    DAILY,      // Her gün
    WEEKLY,     // Haftanın belirli günleri (Örn: Pzt, Çar)
    PERIODIC    // Belirli aralıklarla (Örn: 2 günde 1)
}